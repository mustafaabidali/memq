// Synthetic fixture, not an application implementation.
export const providers = [{ id: "google", enabled: true }, { id: "facebook", enabled: false }];
export function localePortal(locale: string) { return `/${locale}/portal`; }
